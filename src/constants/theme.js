// Mirrors the `bereal` palette registered in tailwind.config.js.
// Kept as plain JS so non-Tailwind consumers (e.g. canvas draw calls
// for a future AR overlay) can reference the same source of truth.
export const theme = {
  black: '#000000',
  surface: '#1c1c1e',
  surface2: '#2c2c2e',
  ink: '#ffffff',
  muted: '#B6B9BF',
  gold: '#F5C518',
  blue: '#3B82F6',
}
